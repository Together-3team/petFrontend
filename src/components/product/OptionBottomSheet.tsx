import useModal from '@/hooks/useModal';

export default function OptionBottomSheet() {
  const { modalOpen, handleModalOpen, handleModalClose } = useModal();

  return <div>OptionBottomSheet</div>;
}
